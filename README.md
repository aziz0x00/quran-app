# Quran App

run with `pnpm dev`

## Keymaps

- `Ctrl+P` for Surah menu
- `Arrows` or `hjkl` for navigation
- `Tab` to change double/single page view

More keyboard-driven navigation (vim) to be added to make navigation easy.
